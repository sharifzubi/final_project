%h1 Users

- if current_user.admin == true
  - @users.each do |user|
    %h3= link_to user.username, user_path(user)
    = link_to user.first_name
- else
  - @users.each do |user|
    %h3= link_to user.username, dashboard_path

  / = link_to user.avatar, dashboard_path(user)
